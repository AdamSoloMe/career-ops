---
phase: 02-ats-resume-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modes/oferta.md
autonomous: true
requirements:
  - ATS-01
  - ATS-03
  - ATS-04

must_haves:
  truths:
    - "Evaluating a job via /career-ops oferta produces a Block I section headed '## I) ATS Analysis' in the report"
    - "Block I shows an overall ATS score (0-100%) computed from three weighted categories: hard skills 50%, job title 30%, soft skills 20%"
    - "Block I identifies matched and missing keywords for each category, with per-keyword placement guidance for hard skills and job title keywords missing from cv.md"
    - "The ATS platform (Greenhouse/Lever/Ashby/Workday/Taleo/iCIMS/SAP SuccessFactors/Unknown) is inferred from the job URL with a one-line strictness note"
    - "If cv.md is absent, Block I outputs 'ATS: N/A (cv.md not found)' and skips the block gracefully"
    - "Block H (Draft Application Answers) remains at '## H) Draft Application Answers' — untouched and unrenamed"
    - "The report header includes '**ATS:** {score}% ({platform}) — Missing: {kw1}, {kw2}, {kw3}' between **Score:** and **Legitimacy:**"
  artifacts:
    - path: "modes/oferta.md"
      provides: "Block I prompt instructions + updated report header template"
      contains: "## Bloque I — Análisis ATS"
  key_links:
    - from: "Block I prompt"
      to: "cv.md"
      via: "read at evaluation time (same as Block B)"
      pattern: "cv.md"
    - from: "report header template"
      to: "**ATS:** field"
      via: "inserted between **Score:** and **Legitimacy:**"
      pattern: "\\*\\*ATS:\\*\\*"
---

<objective>
Add ATS Analysis as Block I in `modes/oferta.md` — the core scoring block that grades the candidate's resume against each job description using a 3-category weighted rubric, infers the ATS platform from the job URL, and produces actionable placement guidance for each missing keyword.

Purpose: Every job evaluation now includes a machine-readable quality signal (ATS %) alongside the human-readable offer score. The candidate knows before applying whether their resume will pass automated screening and exactly which keywords to add — and where.

Output: Updated `modes/oferta.md` with Block I appended after Block H and the report header template extended with the `**ATS:**` line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Key current state of modes/oferta.md — executor must know exact lines before editing. -->

From modes/oferta.md (lines 143–198) — POST-EVALUATION section (do NOT modify Block H):
```markdown
## Post-evaluación

**SIEMPRE** después de generar los bloques A-G:

### 1. Guardar report .md

...

**Formato del report:**

```markdown
# Evaluación: {Empresa} — {Rol}

**Fecha:** {YYYY-MM-DD}
**Arquetipo:** {detectado}
**Score:** {X/5}
**Legitimacy:** {High Confidence | Proceed with Caution | Suspicious}
**PDF:** {ruta o pendiente}

---

## A) Resumen del Rol
...
## G) Posting Legitimacy
(contenido completo del bloque G)

## H) Draft Application Answers
(solo si score >= 4.5 — borradores de respuestas para el formulario de aplicación)

---

## Keywords extraídas
(lista de 15-20 keywords del JD para ATS optimization)
```
```

Block H is at line 191 — it stays at `## H) Draft Application Answers`. Block I is appended AFTER Block H in the report format template.

The report header currently ends at `**PDF:** {ruta o pendiente}`. The `**ATS:**` line must be inserted between `**Score:**` and `**Legitimacy:**`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Block I prompt section to modes/oferta.md</name>
  <files>modes/oferta.md</files>
  <read_first>
    modes/oferta.md — read the FULL file before editing. Identify: (a) end of Block G section (~line 143), (b) Post-evaluación section start (~line 145), (c) the report header template block (~lines 159-166), (d) Block H at ~line 191, (e) end of the report format template block. You will make two edits: one to insert the Block I prompt, one to update the report header template.
  </read_first>
  <action>
Make exactly two edits to modes/oferta.md:

**EDIT 1 — Insert Block I prompt between Block G section and "## Post-evaluación".**

After the closing "---" of Block G and before "## Post-evaluación", insert the following block:

```markdown
## Bloque I — Análisis ATS

Lee `cv.md` (ya cargado para Bloque B). Si `cv.md` no existe, escribir `**ATS:** N/A (cv.md not found)` y omitir el bloque completo.

### Extracción de keywords del JD

Extrae keywords en tres categorías. Priorizar secciones "required" y "preferred". Ignorar boilerplate genérico (beneficios, equal opportunity, ubicación, visa).

**Hard skills (peso: 50%):** Herramientas, frameworks, lenguajes, plataformas, metodologías explícitamente nombradas (ej: Python, LangChain, Kubernetes, RAG, CI/CD). Máximo 15–20 términos más específicos.

**Job title keywords (peso: 30%):** Palabras del título del rol y modificadores de seniority (ej: "Senior", "Staff", "Lead", "Head of", "Engineer", "Manager", "Architect").

**Soft skills / action verbs (peso: 20%):** Verbos de acción en bullets de requisitos (ej: led, built, shipped, mentored, owned, collaborated) y señales soft (cross-functional, stakeholder, communication, strategic).

### Puntuación por categoría

Para cada keyword extraída, evaluar contra `cv.md`:
- **Match completo (1.0):** Keyword exacta o sinónimo directo presente en cv.md
- **Match parcial (0.5):** Concepto relacionado presente pero con terminología diferente (ej: JD dice "LangChain", CV dice "AI agent orchestration framework" — anotar como "(partial)")
- **Ausente (0.0):** Concepto no encontrado en cv.md

Score de categoría = (suma de match scores) / (total keywords extraídas) × 100%

**Score global** = (hard_score × 0.50) + (title_score × 0.30) + (soft_score × 0.20) → redondear al entero más cercano

### Inferencia de plataforma ATS

Detectar plataforma por URL del JD:

| URL contiene | Plataforma | Strictness |
|-------------|------------|------------|
| greenhouse.io | Greenhouse | Moderate — fuzzy + semantic matching |
| lever.co | Lever | Flexible — stemming-based, most forgiving major ATS |
| ashbyhq.com | Ashby | Criterion-based — binary Meets/Does-not-Meet per recruiter criteria; keyword density less relevant |
| myworkdayjobs.com / workday.com | Workday | Moderate — NLP understands synonym equivalence |
| taleo.net | Taleo | Strictest — exact keyword match only, no synonyms |
| icims.com | iCIMS | Moderate — keyword + skills taxonomy matching |
| successfactors.com / sapsf.com | SAP SuccessFactors | Moderate-strict — structured fields matter, NLP limited |
| (ningún match) | Unknown | Assume moderate strictness |

### Output del Bloque I

**ATS Score:** {score}% ({plataforma} — {strictness one-liner})

#### Breakdown por categoría

| Categoría | Matched | Missing | Score |
|-----------|---------|---------|-------|
| Hard skills (50%) | {lista, comma-separated} | {lista} | {x}/{n} = {%}% |
| Job title (30%) | {lista} | {lista} | {x}/{n} = {%}% |
| Soft skills / action verbs (20%) | {lista} | {lista} | {x}/{n} = {%}% |

**Overall:** ({hard}% × 0.50) + ({title}% × 0.30) + ({soft}% × 0.20) = **{global}%**

#### Guía de keywords faltantes

Para cada keyword ausente de **hard skills** y **job title**, identificar dónde en cv.md añadirla naturalmente:

**Missing: {keyword}** — Añadir a "Experience > {Empresa} > {bullet específico que ya existe}".
Sugerencia: "...{frase natural integrando la keyword en experiencia existente — NUNCA inventar experiencia}..."

Para soft skills / action verbs ausentes (sin guía de placement específica):
- Missing soft skills: {lista}

**Regla crítica:** Guiar únicamente hacia reformulación de experiencia REAL. Nunca sugerir añadir skills que el candidato no tiene.
```

**EDIT 2 — Update the report header template inside the "### 1. Guardar report .md" section.**

Find the report format template block (the markdown code fence containing `**Score:** {X/5}` and `**Legitimacy:**`). Replace the header lines:

BEFORE:
```
**Score:** {X/5}
**Legitimacy:** {High Confidence | Proceed with Caution | Suspicious}
**PDF:** {ruta o pendiente}
```

AFTER:
```
**Score:** {X/5}
**ATS:** {score}% ({platform}) — Missing: {kw1}, {kw2}, {kw3}
**Legitimacy:** {High Confidence | Proceed with Caution | Suspicious}
**PDF:** {ruta o pendiente}
```

**EDIT 3 — Add Block I to the report sections list inside the same template.**

Find the section in the report format template that lists the blocks (## A) through ## H)). After the `## H) Draft Application Answers` entry, add:

```
## I) ATS Analysis
(contenido completo del bloque I)
```

This keeps it before the closing `---` and the `## Keywords extraídas` section.

**Important constraints:**
- Block H heading `## H) Draft Application Answers` must remain exactly as-is at its current line
- Do not alter any content in Blocks A–G or Block H
- Do not change the opening line of modes/oferta.md or the Paso 0 / archetype detection section
  </action>
  <verify>
    Run: grep -n "Bloque I" modes/oferta.md
    Expected: at least one match showing "## Bloque I — Análisis ATS"

    Run: grep -n "\*\*ATS:\*\*" modes/oferta.md
    Expected: at least two matches — one in the Block I output section, one in the report header template

    Run: grep -n "## H) Draft Application Answers" modes/oferta.md
    Expected: exactly one match at the same line number as before (confirm Block H was not moved)

    Run: grep -n "Bloque I\|## I)" modes/oferta.md
    Expected: Block I appears AFTER Block H (higher line number)

    Run: grep -A1 "\*\*Score:\*\*" modes/oferta.md | grep "ATS"
    Expected: the line immediately after **Score:** contains **ATS:**
  </verify>
  <acceptance_criteria>
    - `grep "## Bloque I — Análisis ATS" modes/oferta.md` exits 0
    - `grep "\*\*ATS:\*\*" modes/oferta.md` returns at least 2 lines
    - `grep "## H) Draft Application Answers" modes/oferta.md` returns exactly 1 line (not moved/duplicated)
    - `grep "## I) ATS Analysis" modes/oferta.md` exits 0 (block appears in report sections list)
    - `grep "taleo.net" modes/oferta.md` exits 0 (platform table present)
    - `grep "successfactors.com" modes/oferta.md` exits 0 (iCIMS/SAP platforms included)
    - `grep "NUNCA inventar" modes/oferta.md` exits 0 (ethical guardrail present)
    - `grep "cv.md not found" modes/oferta.md` exits 0 (graceful degradation present)
    - Line order: `grep -n "Score:\|ATS:\|Legitimacy:" modes/oferta.md` shows Score line number < ATS line number < Legitimacy line number (within the report header template block)
  </acceptance_criteria>
  <done>modes/oferta.md contains Block I with the full 3-category scoring rubric, platform inference table (8 entries including iCIMS and SAP SuccessFactors), per-keyword placement guidance, graceful cv.md-missing handling, and the report header template updated with **ATS:** between **Score:** and **Legitimacy:**. Block H is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JD text → LLM scoring | Untrusted JD content processed by Claude; no code execution path |
| cv.md read → Block I output | User's CV read at evaluation time; must never be written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Block I keyword guidance | accept | Guidance is additive (suggest rephrasing existing experience); no sensitive data exposed beyond what's in cv.md which the user owns |
| T-02-02 | Tampering | cv.md read path | mitigate | Block I prompt includes explicit "NUNCA escribir en cv.md" rule; cv.md is read-only throughout the scoring block |
| T-02-03 | Denial of Service | Missing cv.md | mitigate | Graceful degradation: output "ATS: N/A (cv.md not found)" and skip block rather than crash evaluation |
| T-02-04 | Spoofing | ATS platform inference | accept | Platform inference is heuristic from URL pattern; if URL is obfuscated, falls back to "Unknown / moderate strictness" which is a safe default |
</threat_model>

<verification>
After this plan executes:
1. `grep "## Bloque I" modes/oferta.md` — must return a match
2. `grep "\*\*ATS:\*\*" modes/oferta.md` — must return ≥ 2 matches
3. `grep -c "## H) Draft Application Answers" modes/oferta.md` — must return exactly 1
4. `node test-all.mjs` — must still pass (no new assertions yet; existing 63+ checks must not regress)
</verification>

<success_criteria>
- modes/oferta.md contains Block I with: 3-category weighted scoring rubric (hard 50%, title 30%, soft 20%), platform inference table (8 platforms), per-keyword placement guidance with ethical guardrail, graceful cv.md-missing handling
- Report header template in modes/oferta.md shows **ATS:** field between **Score:** and **Legitimacy:**
- Block H (Draft Application Answers) remains at its original heading and line, untouched
- `node test-all.mjs` passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-ats-resume-scoring/02-01-SUMMARY.md`
</output>
